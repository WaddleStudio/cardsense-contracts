title = "CardSense Contracts secret scanning"

[extend]
useDefault = true

[[allowlists]]
description = "Documented placeholder secrets in examples and tests"
regexes = [
  '''<[^>\n]*(?:key|token|secret|password)[^>\n]*>''',
]
